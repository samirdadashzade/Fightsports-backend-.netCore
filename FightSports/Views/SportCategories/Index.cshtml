@model IEnumerable<FightSports.Models.SportCategories>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/admin_panel.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SportCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SportCategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SportCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SportCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SportCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
